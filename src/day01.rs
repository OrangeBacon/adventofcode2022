use anyhow::Result;
use libaoc::{aoc, AocResult, Timer};

#[aoc("", "")]
pub fn solve(timer: &mut Timer, input: &str) -> Result<AocResult> {
    timer.lap("Parse");

    timer.lap("Part 1");

    timer.lap("Part 2");

    Ok(AocResult::new("", ""))
}
